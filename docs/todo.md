# TODO

- use `realpath` consistently in `restartnow()` and closing `if [[ $CHANGES -gt 0 ]]` check
- consider using some functions (sorry Steve), e.g. for checking if a pacman package is installed
- make all vars lowercase
- normalize use of `arch-chroot /mnt ...` vs `/mnt...`
- normalize use of `2>/dev/null` vs `2>/dev/null`
